# 代办列表

> 项目待办与已知技术债。完成一项移到下方「已完成」，保留一行结论。

## TODO

### 1. 问答 BM25 路：查询时重切块 + 全量 overlap 打分，性能差

- **现状**（`ModeSearcher#recallChunks()` ①，`backend/src/main/java/com/docrag/searcher/ModeSearcher.java`）：
  每次问答请求，BM25 路先全库按 doc 召回 pool=clamp(bm25Chunks, 5, 20) 篇（`topDocsWithContent` 含 content 全文）→ 对每篇**查询时重新按入库策略切块**（`chunkForMode()`）→ 对每个 chunk 用 IKAnalyzer 细粒度分词做 overlap 计数（`overlap()`），score>0 才入围再排序。
- **为什么差**：
  1. 切块与分词是纯 CPU 重复劳动——同一文档每次被问答命中都重切一遍、每个 chunk 重新分词，无任何缓存；
  2. doc 级 BM25 粒度粗：一文一 Document，长文档整篇 content 读出（存储字段大，IO 也贵），BM25 对长文本的词频区分度也弱；
  3. pool 上限 20 篇全文 × 全部 chunk 逐个分词，问题稍长（问题也要分词）开销线性放大。
- **候选方案**（实施前需评估，不必一步到位）：
  - **A. chunk 级倒排（根治）**：入库时把 chunk 作为独立 Document 写入倒排（`chunkId/docId/序号/text`），查询直接 chunk 级 BM25——查询时重切与 overlap 打分整体消失；副作用是倒排库从「一文一 Document」变「一 chunk 一 Document」，`search()` 检索页链路与 schema 需同步改（CLAUDE.md §4 indexer schema、§5 契约）。
  - **B. 进程内缓存（保守）**：按 `(mode, docId)` 缓存切块结果与 chunk 分词词项集，doc 未变（上传/删除时失效）不重算；不动 schema，只消重复计算。
  - A、B 不互斥：先 B 缓解，A 作为检索质量与性能的长期形态。
- **约束提醒**：
  - 「检索期切块 = 入库期切块」契约（CLAUDE.md §4 searcher）是 chunk 级去重成立的前提——方案 A 等于把切块固化到入库期，契约从「两侧同步调用同一 Chunker」变为「入库期唯一」，需同步改 CLAUDE.md 与《解析与入库》§4.3；
  - 改动后用 `eval/run_eval.py` 复测双模式通过率，确认召回质量无回归（尤其 plain 表格聚合题对 bm25Chunks 预算敏感）。
- **评测证据（2026-09-19 corpus_complex）**：doc 池挤占已实测定性——公司名类查询下 top-5 doc 池被大年报词频占满，大 xlsx 目标行 chunk 进不了池，表格定位题 plain 侧 0/7（详见 `eval/results/eval-20260919-071349.md` §3①、交接文档 `eval/任务交接-corpus_complex评测.md`）。

## 已完成

（暂无）
